# tmux mouse mode is intentionally OFF.
# Enabling it (set -g mouse on) breaks text selection in the browser-based
# xterm.js terminal because tmux captures all mouse events. Without mouse
# mode, xterm.js handles selection natively and Cmd+C copies to clipboard.
